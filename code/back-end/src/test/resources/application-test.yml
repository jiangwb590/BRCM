spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bcrm?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: diandian812

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: bcrm-test-secret-key-for-unit-testing-purpose-only-2024-bcrm-testing
  expiration: 86400

logging:
  level:
    com.bcrm: DEBUG
    org.springframework: INFO